kind: Service
apiVersion: v1 
metadata:
  name: blog-svc 
  namespace: blog
spec:
  type: NodePort
  selector:
    app: blog-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 80
      nodePort: 30002
