apiVersion: v1
kind: Service
metadata:
  name: blog-lb
  namespace: blog
spec:
  type: LoadBalancer   
  selector:
    app: blog-frontend
  ports:
    - protocol: TCP
      port: 80          # External public port (e.g., http://your-lb-ip)
      targetPort: 80    # Port inside the container